# =============================================================================
# SmartPlay FPL Backend - Environment Variables
# =============================================================================
# Copy this file to .env for local development
# DO NOT commit .env to version control
#
# IMPORTANT: Production environment variables are set in Railway Dashboard,
# NOT in any .env file. This ensures complete separation.

# =============================================================================
# ENVIRONMENT IDENTIFIER
# =============================================================================
# Values: "development" | "production"
ENVIRONMENT=development

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
HOST=0.0.0.0
PORT=8000

# Debug mode - MUST be false in production
DEBUG=true

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=DEBUG

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Frontend URL for CORS
# - Development: http://localhost:3000
# - Production: https://smartplayfpl.com (set in Railway)
FRONTEND_URL=http://localhost:3000

# Additional CORS origins (comma-separated)
# Production: Add Vercel preview URLs if needed
CORS_ORIGINS=

# =============================================================================
# DATABASE
# =============================================================================
# - Development: SQLite (local file) - no setup required
# - Production: PostgreSQL on Railway (DATABASE_URL auto-injected)
#
# Railway Setup:
# 1. Go to Railway Dashboard > Your Project
# 2. Click "+ New" > "Database" > "PostgreSQL"
# 3. Click your backend service > Variables
# 4. Add Variable Reference: select DATABASE_URL from PostgreSQL
# 5. Railway auto-provides: postgres://user:pass@host:5432/railway
#
# Note: Code auto-converts postgres:// to postgresql:// for SQLAlchemy
DATABASE_URL=sqlite:///./smartplayfpl.db

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================
# Claude AI API key (required for AI features)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# =============================================================================
# FEATURE FLAGS
# =============================================================================
# Skip Knowledge Graph initialization (faster startup in dev)
SKIP_KG=false

# Skip ML scheduler
SKIP_SCHEDULER=false

# =============================================================================
# PRODUCTION ENVIRONMENT (Railway Dashboard)
# =============================================================================
# Set these in Railway Dashboard > Project > Variables:
#
# ENVIRONMENT=production
# DEBUG=false
# LOG_LEVEL=INFO
# FRONTEND_URL=https://smartplayfpl.com
# CORS_ORIGINS=https://smartplayfpl.vercel.app,https://www.smartplayfpl.com
# DATABASE_URL=(Railway provides this automatically for PostgreSQL)
# ANTHROPIC_API_KEY=your_production_api_key
